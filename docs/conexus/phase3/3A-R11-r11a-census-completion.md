# 3A-R11-A — Authority Census Completion

**Status:** R11-A COMPLETE / RECONCILIATION INPUT  
**Authority:** R11 working Evidence; not final current-state product authority  
**Parent:** [3A-R11](3A-R11-whole-product-authority-rebaseline.md)  
**Census:** [3A-R11 Authority Census](3A-R11-authority-census.md)  

## 1. Completeness

The required corpus for R11-A has been located and compiled at the level needed to begin claim-level Decision Reconciliation:

```text
C-000..C-017                          COVERED
3A current reconciliation set         COVERED
3B-01..3B-17                         COVERED
3C..3K accepted closure families      COVERED
3L-Q0 + Package A                    COVERED
```

No dialogue/review/research artifact was promoted to authority by presence.

## 2. Targeted hotspot verification

### H1 — Builder runtime generation

**VERIFIED SCOPED SUPERSESSION.**

```text
C-002/C-017 Pi/fresh-worker realization
→ superseded for F1 runtime realization by 3A-R5 + 3H-01
```

Survives:

```text
Hub sovereignty
CodingWorkerRuntime narrow replaceability boundary
Change / WorkUnit / ActorRun semantic separation
bounded execution/audit/budget facts
fresh independent verification when material
```

Superseded:

```text
Pi as primary F1 coding runtime
fresh cognitive reset per WorkUnit/ActorRun
```

Current Builder cognitive/runtime line:

```text
Change
→ persistent Change-scoped CodingSession
→ Mastra AgentController
→ Mastra Workspace
→ E2B
```

### H2 — Builder model credential custody

**VERIFIED EXPLICIT SUPERSESSION.**

3I-02 explicitly records:

```text
guest LLM key = DELETED
```

Current law:

```text
model-provider credential = control-side owner credential
no model-provider credential in E2B guest
```

The broader bounded guest-readable capability concept survives only for separately admitted non-model capabilities and never grants guest mint/widen/refresh authority.

### H3 — Product Agent runtime

**VERIFIED EXPLICIT SUPERSESSION OF REALIZATION, PRESERVATION OF PRODUCT SEMANTICS.**

```text
C-010 Vercel AI SDK/light-loop realization
→ superseded by 3H-02
```

Current line:

```text
exact Release composition
→ RuntimeAgentProjection
→ direct Mastra Agent
```

Preserved:

```text
git-first Product Agent definition/revision
Conversation / AgentRun / ApprovalRequest meaning
exact tool/effect/approval identity
Gateway effect authority
budget/model pinning requirements
```

Rejected current inheritance:

```text
Stored Agent/latest authority
Editor override authority
universal Workflow baseline
framework state as Product Agent truth
```

### H4 — pg-boss / schedule authority

**VERIFIED CANDIDATE-ONLY STATUS.**

3A-R9 makes scheduler/queue reconstructible mechanics. 3L-Q0 pins:

```text
pg-boss 12.26.3 = incumbent Package D candidate
```

It is not schedule authority and is not qualified yet. Package D must prove the current managed-job laws, especially:

```text
one catch-up after downtime
not N replayed missed slots
```

No current document may present pg-boss as permanent/qualified product architecture before Package D adjudication.

### H5 — Published App access / C-015

**VERIFIED PARTIAL SUPERSESSION WITH IMPORTANT PRESERVATION.**

3B-14/3C-02 establish:

```text
CONTROL_PLANE
!= PREVIEW
!= PUBLISHED_APP authorization
```

Therefore old `ProjectMembership` wording cannot remain a combined Control Plane + Published App authority.

However the 3B cross-review explicitly preserves C-015's closed published-app role set:

```text
{admin, member}
```

until a later material decision changes it. Illustrative role names in 3B-14 do not amend that set.

Preserved C-015 security/identity intent includes one global Account, server-side authority resolution, opaque session/no browser bearer, deny-by-absence and server-derived resource/surface.

Physical/local-serving wording is governed by 3J, not C-015 in isolation.

### H6 — Observability versus authoritative result truth

**VERIFIED EXPLICIT SEPARATION.**

3H-03 freezes:

```text
owner-control F5 proposal
!= Operational Telemetry observation
```

And:

```text
runtime emits complete trace
-X-> owner terminal fact
```

F5 target identity derives from owner dispatch context/opaque handle; producer payload is cross-check only. Duplicate application remains owner/write-once safe. Telemetry, provider state, trace IDs and guest observation never manufacture owner truth.

3K-02 carries the same law into the UI: model narration, telemetry and visual state cannot become domain/security/verification authority.

### H7 — Brain versus runtime memory

**VERIFIED EXPLICIT SEPARATION.**

3C-09 freezes:

```text
Brain
!= Conversation memory
!= AgentRun history
!= Builder scratchpad
!= tasks.md
!= personal user memory
```

Brain remains Workspace-scoped semantic/knowledge/evidence authority with git-first publication + Registry revision + Project binding semantics.

3A-R10 and 3L-A confirm:

```text
memory framework never Brain authority
Builder OM evaluated, KEEP OFF
Product Agent extra memory/semantic recall/extractors = consumer/eval-gated
```

### H8 — Future seams versus dormant F1 machinery

**VERIFIED AS CROSS-CUTTING CURRENT LAW.**

The accepted corpus repeatedly applies:

```text
prepare the seam, not the future capability
```

Current examples include:

```text
DEDICATED physical deployment       deferred until first real DEDICATED consumer
multi-repo Project                  deferred until real consumer
cross-Workspace reuse/exchange      explicit future capability only
Product Agent advanced memory       consumer/eval-gated
Durable Agent                        deferred until reconnectable same-stream consumer
EVENT trigger                        reserved, not operational F1
generic workflow/scheduler domain    rejected F1
external Vault/KMS/HSM               deferred until security/compliance failure class
Connection pools/failover            deferred until real need
public/embed/self-signup/SSO/SCIM    trigger-based, not dormant baseline machinery
```

R11 current projections must preserve these named seams/triggers without materializing empty modules/tables/services/config/frameworks.

## 3. Potential contradiction check — first production

A wording check was performed between 3A-R10's current realization map and 3J closure.

Result:

```text
material contradiction = NONE
```

3J-R1 confirms the same first-installation class:

```text
on-prem / single-host first installation
company LAN / existing corporate VPN + HTTPS
no public Internet ingress F1
```

and explicitly states that this is first-deployment evidence, **not universal SaaS/product topology**.

## 4. R11-A verdict

```text
required corpus found                         = YES
high-risk supersessions checked               = H1..H8 COMPLETE
irreconcilable accepted-authority conflict    = NONE FOUND
material REOPEN required by census            = NONE
R11-B Decision Reconciliation                 = ADMITTED / NEXT
Package B                                     = PAUSED / NOT OPENED
product implementation                        = BLOCKED
```

This does not mean every historical sentence is current. It means the identified overlaps can proceed to R11-B as explicit `CURRENT/PRESERVE/REFINED/PARTIALLY_SUPERSEDED/SUPERSEDED/DEFERRED/REJECTED_F1` dispositions without first reopening architecture.

## 5. Exact next action

> **R11-B — build the candidate Decision Reconciliation Registry, claim the current generation of each material decision/family, and make stale mechanism inheritance mechanically obvious before deriving Product Contract or Architecture Baseline.**
