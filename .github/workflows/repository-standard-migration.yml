name: Repository Standard Migration

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  migrate:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo paused
