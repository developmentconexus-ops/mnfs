# 3A Fable Dialogue — Pre-3K Global Platform Coherence Checkpoint

**Status:** NON-AUTHORITATIVE REVIEW INPUT  
**Phase:** 3A continuous Architecture Reconciliation — extraordinary checkpoint before 3K  
**Purpose:** verify that C-001..C-017 + approved Phase 3 through CLOSED 3J still describe the right Conexus platform, have assimilated references/research coherently, have not accumulated accidental complexity or silent supersession, and leave the correct critical path into 3K..3O. This file does not create authority, reopen a phase by itself, authorize implementation, close C-018, merge, or PR readiness.

---

## 1. Why this checkpoint exists

The system has now passed through substantial product research and architecture:

```text
Mitra field evidence
Factory / agentic software-factory research
MNFS lessons
FirstMate historical patterns
Mastra qualification/reconciliation
E2B sandbox choice
PostgreSQL/data architecture
C-000..C-017 product decisions
3B..3J architecture
```

Before entering `3K — Frontend / Product Architecture`, there is a specific risk:

> we could have built a technically rigorous architecture that is internally coherent but no longer optimizes for the product Conexus was meant to become.

This checkpoint therefore asks a different question from later `3N`:

```text
THIS CHECKPOINT
→ are we still building the right platform?
→ were references used as evidence rather than copied?
→ did architectural refinement preserve the original product thesis?
→ did we add complexity only where a real authority/failure class required it?
→ are strategic F1 requirements still owned/routed?

3N LATER
→ final architecture verification after 3K/3L/3M
→ duplicate/missing authority, contradictions, recovery, evidence sufficiency
```

Do not replace or weaken 3N with this review.

---

## 2. Canonical state to reconstruct independently

Do not trust this dialogue as authority. Read `AGENTS.md` and canonical chain.

Expected current state after operator ratification:

```text
3A = CONTINUOUS / 3A-R6 APPROVED
3B = CLOSED / APPROVED
3C = CLOSED / APPROVED
3D = CLOSED / APPROVED
3E = CLOSED / APPROVED
3F = CLOSED / APPROVED
3G = CLOSED / APPROVED
3H = CLOSED / APPROVED
3I = CLOSED / APPROVED
3J = CLOSED / APPROVED by 3J-R1
3J-04 = NOT JUSTIFIED
3K..3O = NOT STARTED
```

`LEDGER.md` may still require mechanical projection of the already-ratified `3J-R1`; that is status synchronization only, not a checkpoint decision.

Implementation remains prohibited until:

```text
3K..3O complete
→ C-018
→ F3B-R1 canonical product repo/cutover gate
→ post-C-018 Realization Planning
→ accepted executable plan(s)
→ product implementation
```

---

## 3. Product thesis — what Conexus must still be

C-001 remains product vision authority.

> **Conexus is a unified AI-first platform that builds and operates business applications integrated with ERPs, agent-guided and supported by deep company knowledge.**

The architecture must support two connected product roles without splitting into two factories:

```text
APPLICATION PLATFORM
→ companies build/run internal business apps and agents
→ reuse Workspace Brain, Connections, governed capabilities
→ MANAGED runtime is the natural Golden Path

SOFTWARE FACTORY
→ operator builds independent commercial software/products
→ same Project/Change/Builder/verification/Release model
→ DEDICATED runtime seam exists when independent runtime is actually required
```

3C-12 explicitly realizes this as:

```text
ONE Software Factory + Application Platform
same Factory / Project / Change / Builder / Release
ApplicationRuntimeProfile = MANAGED | DEDICATED
```

This is a major coherence success and must not be weakened in 3K.

### C-001 pillars that must remain visible in the resulting platform

```text
P1 — Company Brain
P2 — engineering embedded in template/gates, not governance bureaucracy
P3 — AI-first native platform

C1 — insight → action / autonomous Product Agents
C2 — golden benchmark / builder regression quality
C3 — visible cost/context/evidence
C4 — platform consultant dogfoods platform knowledge mechanism
```

---

## 4. Platform map after 3J

The approved architecture currently composes into this system:

```text
ACCOUNT
  ↓
WORKSPACE / GROUP
  ├── Areas / organizational access
  ├── Canonical Brain
  └── Connections
          │
          │ explicit bindings
          ▼
PROJECT
  ├── Baseline / runtimeProfile MANAGED|DEDICATED
  ├── Project DB / data model
  ├── ProjectBrainBinding
  ├── ProjectConnectionBinding
  │
  ├── BUILD PLANE
  │   Change
  │     ↓
  │   correctness / planning
  │     ↓
  │   Builder / CodingSession
  │     ↓
  │   WorkUnit / ActorRun
  │     ↓
  │   E2B
  │     ↓
  │   verification / Findings / Evidence
  │
  ├── ARTIFACT / RELEASE PLANE
  │   Git
  │     ↓
  │   Artifact Registry / immutable revisions
  │     ↓
  │   Release / Promotion / active pointer
  │
  └── PRODUCT PLANE
      MANAGED app → MAR
      Product Agent → PAR
           │
           └── tools / integrations / data effects
                    ↓
             Capability Gateway
                    ↓
             Connections / Project Data / external systems

OBSERVABILITY & AUDIT
→ crosses the system as evidence/history
→ never replaces domain authority
```

This is not several products glued together. It is one platform with explicit planes and one Project/Release composition root.

---

## 5. C-003 requirement-family coverage audit

This checkpoint checks product requirements, not only architecture phases.

### HAR — Builder / Harness

Current coverage:

```text
Project/Change/correctness        → 3B / 3C-05 / 3G
planning proportionality          → 3B-07 / 3G-04 / C-017
persistent coding harness         → 3A-R5 / 3H-01
E2B sandbox                       → C-008 / 3H-01
mechanical gates / verification   → C-012 / C-017 / 3H / 3N/3O
human checkpoints                 → C-017 / 3G
```

C-002/C-017 mechanism pins that became stale were explicitly reconciled by 3A-R5.

### REG / DAT / INT

```text
Artifact Registry / immutable revs  → 3C-06 / 3E / 3F
Project data / migrations            → C-006 / 3E / 3G / 3J
integration abstraction              → C-007 / 3C-07 / 3C-08
Connection credential custody        → 3I-02
business/application effects         → Gateway / 3G-06
```

Sankhya remains first connector/consumer, not architecture authority.

### CER — Company Brain

```text
Group terminology     → technically Group, product Workspace
Brain scope            → Workspace-scoped 0..1 canonical Brain
semantic/knowledge/evidence → 3C-09
Project reuse          → explicit ProjectBrainBinding
machine-propose/human-publish → Brain
runtime use            → Builder + PAR context
```

P1 remains structurally strong and has not collapsed into project-local prompt files.

### AGT — Agents

```text
AGT-1 first-class agent          → 3C-10 / Registry / PAR
AGT-2 agent management           → PAR domain; product surface remains 3K
AGT-3 headless Product Agent     → SCHEDULE F1 in 3H-02; EVENT/webhook remains consumer-gated through C-007
AGT-5 interactive conversation   → Conversation/AgentRun runtime + 3K surface
```

**AGT-4 is the one current coverage question; see Finding Candidate F-GPC-01.**

### PUB / CIC / OBS / SEG / QUA

Core ownership exists:

```text
published runtime / auth           → MAR + I&A + C-015 + 3I + 3J
Release/Promotion                  → Release + 3G-08
runtime evidence                   → OBS / Verification Observability
security / authority               → 3I
builder qualification              → 3L
architecture coherence             → 3N
vertical proof                     → 3O
```

User-facing realization remains correctly owned by 3K.

---

## 6. Supersession audit — old mechanism pins versus current authority

One of the largest risks in a long architecture program is two canonical texts both sounding current while describing different mechanisms.

Current audit:

| Earlier decision | Earlier mechanism | Current reconciliation | Checkpoint result |
|---|---|---|---|
| C-004 | local/SRT sandbox path | C-008 explicitly supersedes with E2B microVM | CLEAN |
| C-002 | Pi principal Builder runtime; fresh worker/WU | 3A-R5 explicitly partially supersedes; Mastra Code/AgentController + Change-scoped session | CLEAN |
| C-017 | Pi fresh material validator | 3A-R5 explicitly preserves freshness law but supersedes mechanism to fresh Mastra verification session | CLEAN |
| C-010 | lightweight custom loop + Vercel AI SDK; reject agent framework | 3C-10 explicitly says rejection is SUPERSEDED for Production Agents; 3H-02 direct Mastra Agent | CLEAN |
| C-013 | `agent_event` agent-centric model | 3C-13 explicitly generalizes to platform-wide Operational Telemetry + separate Audit Trail; 3E-02 closes records | CLEAN |
| C-015/C-016 | tailnet-specific first remote realization | 3J-01 explicit bounded amendment to company-private LAN/VPN + HTTPS | CLEAN |

No silent technology drift was found in these high-risk transitions.

### Documentation legibility risk

The old C-* documents remain historical authority with later amendments/supersessions layered on top. That is method-correct but increases fresh-actor misread risk.

Candidate consolidation rule for C-018:

> C-018 should contain a compact **supersession/amendment index** for mechanism pins changed by Phase 3, without rewriting history or duplicating the full decisions.

This is synthesis/documentation clarity, not a new architecture owner.

---

## 7. Reference assimilation audit

### 7.1 Mitra — ADOPT the leverage, OWN the missing boundaries

Internal measured evidence remains the strongest reference for product directness.

Patterns Conexus correctly preserved/adapted:

```text
project-centered app building
direct app generation
visual/explicit plan and progress
scaffold carrying engineering defaults
server-side integrations / symbolic credentials
real business-data discovery before coding
published real app artifact
```

Gaps Conexus deliberately owns instead of copying:

```text
company-level reusable knowledge
first-class Product Agents
mechanical authority/approval
safe effect boundary
immutable revision/promotion model
independent verification / real runtime evidence
secret/egress custody
honest failure and recovery semantics
```

Checkpoint judgment:

> Conexus has not become “anti-Mitra”; it preserves Mitra's speed/directness structurally, but 3K must prevent internal governance objects from leaking into the default user experience.

### 7.2 Factory — reference, not substrate

Repo research originally captured Factory's 2026-07 public shape: agent readiness, planning, verification and orchestration patterns.

Freshness check on 2026-08-17 against current official Factory docs finds:

- Factory Missions is now documented as a usable structured orchestration workflow with collaborative planning, features/milestones, workers and validation;
- Factory still says planning/validation depth should scale with project complexity and a simple project may use one milestone;
- Factory explicitly states parallelization remains an open question rather than an automatic good;
- Agent Readiness focuses on deterministic repo structure, validation and operational signals;
- Droid Control is explicitly investigator-style real-behavior verification/QA and supports `CONFIRMED/REFUTED/INCONCLUSIVE` evidence.

Primary current references:

- `https://docs.factory.ai/missions/overview`
- `https://docs.factory.ai/missions/planning`
- `https://docs.factory.ai/agent-readiness/overview`
- `https://docs.factory.ai/cli/features/droid-control`

Architecture implication:

```text
Factory evidence STRENGTHENS:
  correctness before execution
  visible plan / validation checkpoints
  repository readiness by construction
  real-behavior QA

Factory evidence DOES NOT JUSTIFY:
  mandatory Mission/Milestone domain
  multi-agent/parallel default
  importing Droid Control/Missions as Conexus authority
  generic readiness score in F1
```

The current repo Factory reference map should eventually be refreshed factually because Missions has evolved since its captured snapshot. This is research hygiene, not an architecture reopen by itself.

### 7.3 FirstMate — historical interaction pattern

Useful transfers remain:

```text
one liaison / coherent visible work
brief before spawn
semantic lifecycle
artifact-based handoff
restart/reconciliation honesty
```

Rejected historical machinery remains correctly rejected where it would duplicate current Builder/Hub authority.

No product dependency should be created from this reference.

### 7.4 MNFS — principles, not kernel inheritance

C-000 remains coherent:

```text
REUSE useful planning/evidence principles
REFERENCE historical lessons
REJECT carrying the old kernel/governance as the Conexus product core
```

Phase 3 correctly re-derived boundaries instead of importing MNFS objects wholesale.

### 7.5 Mastra — current evidence still supports the substrate decision

Phase 3 did not adopt Mastra because it is fashionable; it reopened earlier rejections when its capability surface materially changed.

Current official Mastra evidence remains compatible with the selected seams:

```text
Agents
Workflows
persistent/suspend-resume state
Schedules
Memory
observability / tooling
```

Examples/current sources:

- `https://mastra.ai/agent-builder`
- `https://mastra.ai/blog/introducing-schedules-for-agents-and-workflows`
- `https://mastra.ai/blog/what-are-durable-ai-agents`
- `https://mastra.ai/blog/agent-memory-layers`

The architecture wisely does **not** trust optional framework features without 3L probes. Mastra remains mechanics/substrate; Conexus owns identity, authority, Release, effects, approvals and terminal truth.

### 7.6 E2B — current evidence still supports sandbox boundary

Current official E2B docs continue to provide:

```text
isolated Linux sandbox/VM
secure controller access
network allow/deny controls
restricted public traffic
sandbox lifecycle APIs
```

References:

- `https://e2b.dev/docs`
- `https://e2b.dev/docs/sandbox/secured-access`
- `https://e2b.dev/docs/api-reference/sandboxes/create-sandbox`

This supports the selected E2B direction but does not waive `CX-SBX-E2B-01`; exact behavior remains 3L-qualified.

### 7.7 PostgreSQL version sanity

The architecture froze **PG17 as floor** and requires a common pinned major across relevant environments.

As of 2026-08-17 official PostgreSQL support still lists 17 as supported through 2029 while PostgreSQL 18 is the current stable major.

Reference:

- `https://www.postgresql.org/support/versioning/`

No architecture reopen is justified merely because 18 exists; exact supported major can be selected/qualified in Realization while respecting the floor and compatibility laws.

---

## 8. Complexity / YAGNI audit

At first glance the architecture is large:

```text
15 domain/module boundaries
46 durable record classes
multiple state lifecycles
strong security/recovery laws
```

But complexity must be judged by whether it is essential or accidental.

### Essential complexity that has a real owner/failure class

Examples:

```text
I&A                  → actual tenant/user authority
Workspace            → company/tenant root
Project              → software lifecycle unit
Builder              → software evolution
Registry             → immutable executable definitions
Connections          → reusable external-system binding/credential lifecycle
Gateway              → external/business effect authority
Brain                → cross-Project organizational meaning
PAR                  → Product Agent durable authority
Release              → exact promoted composition
OBS                  → audit/evidence without stealing domain truth
Attachments          → private content custody
MAR                  → real MANAGED serving path
```

The records are physically kept inside one `hub_control` database and modules remain one modular monolith. The design intentionally rejects microservices, workflow engines, policy engines, generic resource graphs, event sourcing, K8s, HA and future-scale machinery.

Checkpoint judgment:

> **The internal model is sophisticated, but not presently overengineered relative to the declared product/trust/effect/recovery problem. The greater risk is PRODUCT-SURFACE complexity, not backend module count.**

### Product-surface guardrail for 3K

The Golden Path must not make a normal user understand:

```text
WorkUnit IDs
ActorRun internals
TxScope
Tier-2 FKs
Promotion FSM steps
producer_trust taxonomy
RPO mechanics
DB role partitioning
```

unless that detail is useful for diagnosis/advanced operation.

A simple build/change should feel closer to:

```text
Describe intent
→ Conexus understands/discovers
→ concise review/approval
→ builds visibly
→ preview
→ publish
```

not:

```text
fill architecture/governance forms
→ manage internal state machines
→ manually coordinate workers
```

C-017 allows this because `FAST` checkpoint is compact and PlanningDepth can be DIRECT. 3K must make that true in the product.

---

## 9. Strategic differentiation audit

### P1 — Brain: STRONG, but final proof routing needs visibility

C-011 explicitly says the defensible differentiation is composition:

```text
company-scoped
+ cross-project
+ semantics + business/process knowledge
+ evidence/health
+ digest/versioning
+ human-governed learning
+ shared by Builder and Product Agents
```

Critical G1 proof is:

> second Project in the same company starts knowing what the first Project established.

This trigger remains correctly deferred until a second real Project exists; the checkpoint does **not** propose manufacturing a second product merely as ceremony.

However 3N/C-018 must ensure the architecture has not accidentally made G1 impossible, and 3K should make Workspace Brain inheritance/binding legible enough that the product does not appear project-local like Mitra.

### P2 — Engineering in artifacts/gates: STRONG with UX caveat

C-012 + C-017 + Phase 3 preserve engineering as:

```text
scaffold
compiler/gates
correctness/evidence
runtime verification
```

not documentation bureaucracy.

Potential failure is UX implementation, not current domain architecture: exposing all correctness/governance internals to the user would violate the spirit of P2 even if mechanically correct.

### P3 — AI-first: STRONG except AGT-4 coverage question

Builder and Product Agent are both first-class, with distinct authority and runtime semantics.

The remaining question is whether the platform-level consultant itself has an explicit owner/seam.

### C1 — Insight → action

PAR + Gateway + ApprovalRequest + SCHEDULE provide the structural F1 path.

`EVENT`/webhook execution is correctly consumer-gated through C-007/Decision Loop rather than constructed without a real trusted ingress consumer.

### C2 — Golden benchmark

3K begins from C-001 case 1, and 3O derives a vertical architecture proof contract from it. This is correctly still on the critical path.

### C3 — cost/context visible

OBS + 3I-03 provide authority/accounting inputs; actual product presentation remains 3K.

### C4 — platform-consultant dogfood

See F-GPC-01.

---

## 10. First vertical / 3O representation guardrail

The first vertical must prove **Conexus**, not only a better dashboard builder.

C-010 already defines the case-1 Product Agent as living in the published Budget Analyzer and consulting enterprise data. Therefore `Production Agent` is naturally part of the first vertical's existing product authority; it is not optional merely because 3A-R6 says “where part of the vertical.”

Pre-3K input:

```text
case 1 should exercise, at minimum where existing authority already requires it:
Project/Baseline
Change / Builder / E2B
Connection / real enterprise data
Brain-backed semantics
verified Release
MANAGED serving
real Account access
Product Agent conversation/tool read path
Evidence/timeline
```

This does not require an external effect/write in the first vertical unless the product case genuinely needs one.

Cross-Project Brain reuse remains G1-triggered; do not create a fake second product merely for the vertical.

---

## 11. Finding Candidate F-GPC-01 — AGT-4 platform consultant ownership is not explicit

**Materiality candidate:** YES — F1 product requirement + user-observable capability + owner/scope choice would otherwise be left to a future coding actor.

### Earlier authority

C-003 / AGT-4:

```text
Consultor de plataforma:
agente que conhece docs/SDKs/padrões do Conexus
(usa o mecanismo do cérebro)
F1
```

C-001 C4 additionally says the platform consultant should dogfood the same company-knowledge idea over Conexus's own docs.

### Current architecture

The architecture clearly owns:

```text
Builder coding agent → Builder
Product/business agent → Project-scoped PAR / agent artifact
Workspace company knowledge → Workspace-scoped Brain
```

But it does not clearly say what AGT-4 is.

A coding actor could otherwise choose among materially different models:

```text
A. special PLATFORM-scoped Product Agent
B. ordinary Project agent in a hidden Conexus Project/Workspace
C. Builder/Control-Plane assistance mode
D. new global assistant module/principal
E. silently omit/defer AGT-4 despite F1 requirement
```

Those options have different tenancy/authority/context implications.

### Independent smallest candidate

**Candidate C — Builder / Control Plane assistance surface** appears Global Maximum on current evidence:

```text
owner = existing Builder / Control Plane application layer
runtime mechanics = existing Builder/Mastra substrate where useful
purpose = explain Conexus, SDKs, patterns, help scope/build Projects
platform knowledge = published/versioned platform-context resources
Project/company context = only the currently authorized Project/Workspace context already admitted to Builder
```

Explicit negatives:

```text
NO new module
NO new principal
NO PLATFORM-scoped `agent` artifact
NO hidden cross-Workspace access
NO new global agent database/state
NO independent Product Agent lifecycle
```

The “dogfood” principle should mean reuse of the same **compiled/versioned/context-with-provenance discipline**, not forcing Workspace Brain domain content to cross tenant boundaries.

If a later real consumer needs a persistent global platform agent independent of Builder/Change, return to Decision Loop with that scope/trust requirement.

### Challenger questions

Fable must challenge:

1. Is AGT-4 actually already resolved by an approved authority that this checkpoint missed?
2. If not, does Candidate C preserve C-001/C-003 intent, or does it improperly collapse a first-class platform agent into Builder?
3. Should AGT-4 instead be explicitly DEFERRED by amending C-003, because no first vertical needs it?
4. Would a PLATFORM-scoped agent require changing Registry/3E/PAR ownership, and is that justified now?
5. Is “same Brain mechanism” semantically the platform context/compiled knowledge pattern, or was literal Brain reuse a hard product requirement?
6. What is the smallest architecture statement that prevents a coding actor from inventing tenant/global authority later?

Until reviewed/ratified, this is a **candidate finding**, not authority.

---

## 12. Reference-freshness finding — Factory research map

**Materiality:** NO by itself.

The captured Factory map should be refreshed because current official docs present Missions as a usable structured orchestration feature rather than only the earlier research-preview framing.

But the newer evidence does not refute current Conexus decisions:

```text
planning quality matters              → already adopted
validation checkpoints matter         → already adopted
repository readiness matters          → scaffold/gates already own this
real-behavior QA matters               → Verification Observability already owns this
parallel/multi-agent always better     → Factory itself does not claim this
Mission/Milestone domain required      → no evidence
```

Recommended later action: update the research reference map for factual freshness without changing authority unless a new material finding emerges.

---

## 13. C-018 synthesis requirement candidate — supersession index

**Materiality:** documentation/synthesis, not architecture reopen.

Because several early C-* mechanism pins were intentionally superseded, C-018 should visibly summarize:

```text
C-004 → C-008
C-002 Builder mechanism → 3A-R5
C-017 validator mechanism → 3A-R5
C-010 Product Agent mechanism → 3C-10 / 3H-02
C-013 agent-centric observability → 3C-13 / 3E-02
C-015/C-016 remote exposure realization → 3J-01 bounded amendment
```

Purpose:

> a fresh implementation actor should not need archaeology to know which mechanism is current.

Do not rewrite historical files or create duplicate authority.

---

## 14. Remaining critical path quality check

The remaining sequence is appropriately bounded:

```text
this checkpoint
→ resolve only material findings
→ 3K product/frontend architecture
→ 3L six load-bearing technology qualification families
→ 3M structural recovery sufficiency sweep
→ 3N one global architecture coherence review
→ 3O contract-only representative vertical proof
→ C-018 synthesis
→ F3B-R1
→ Realization Planning
```

This is now a **stop condition against more speculative architecture**.

Do not add another broad design phase after this checkpoint. Any new work must attach to:

```text
an admitted material finding
one of 3K..3O
or an explicit existing reopen trigger
```

---

## 15. Independent preliminary verdict

### What is strong / coherent

```text
product thesis                  = PRESERVED
Mitra directness leverage       = PRESERVED structurally
company Brain differentiation   = PRESERVED strongly
software-factory + app-platform = PRESERVED by MANAGED|DEDICATED
Builder authority               = coherent after explicit Mastra reconciliation
Product Agent authority         = coherent after explicit Mastra reconciliation
integrations                    = generic Connector/Connection/Gateway, not Sankhya-specific
security                        = strong but mostly mechanical/invisible; no enterprise-theater platform required
persistence/contracts           = explicit enough to stop coding actors inventing authority
runtime/deployment              = proportional single-host first installation; future topology not frozen
reference usage                 = pattern extraction, not product dependency
anti-overengineering            = active and visible throughout approved decisions
remaining path                  = bounded by 3A-R6
```

### Current concerns

```text
F-GPC-01  AGT-4 platform consultant owner/scope not explicit
3K input  must preserve direct/progressive UX; internal rigor cannot become bureaucracy
3K/3O     first vertical must exercise the actual platform composition already required by case 1
3N/C-018  preserve cross-Project Brain seam and publish supersession index
research  refresh Factory reference map factually
```

### Preliminary architecture outcome

```text
CURRENT STRUCTURE CONFIRMED
WITH ONE BOUNDED MATERIAL FINDING CANDIDATE (F-GPC-01)
AND ROUTED PRODUCT/PROOF GUARDRAILS
```

No evidence currently justifies:

```text
architecture reset
new module family
deleting Brain/Gateway/PAR/Release boundaries
microservice split
Kubernetes/HA/cloud-first topology
Mission/Milestone/multi-agent framework
switching away from Mastra/E2B before 3L probes
abandoning first vertical
```

---

## 16. Fable adversarial mandate

Reconstruct authority independently; do **not** trust this checkpoint's conclusions.

Attack the checkpoint across six lenses.

### A — Product thesis fidelity

Is C-001 still honestly realizable by the approved architecture?

Especially:

```text
application platform + software factory dual role
P1 Brain as central differentiation
P2 engineering-in-artifacts rather than bureaucracy
P3 AI-first Builder + Product Agent
C1–C4
```

Find any pillar/complement that has become orphaned or weakened.

### B — Requirement coverage

Audit C-003 MVP/F1 requirements against 3B..3J + routed 3K..3O.

Do not assume this file found the only gap.

For every missing requirement distinguish:

```text
already resolved under a different name
correctly superseded/amended
correctly routed to 3K..3O
DEFER requiring explicit requirement amendment
actual material architecture gap
```

### C — Reference assimilation

Challenge whether we learned the right lessons from:

```text
Mitra
Factory
MNFS
FirstMate
Mastra
E2B
```

Current Factory evidence may be refreshed from official docs, but new reference capabilities never create requirement authority by themselves.

Question whether Conexus preserved Mitra's directness or overbuilt governance.

### D — Complexity / YAGNI

Attack:

```text
15 modules
46 records
state models
security controls
backup/deployment machinery
Builder governance
```

Find accidental complexity that can be removed **without losing a named invariant/failure class**.

Conversely, do not call essential authority/security/recovery complexity “overengineering” merely because it is numerous.

### E — Supersession / contradiction

Independently audit old-to-new mechanism pins. Find any pair of approved canonical authorities that still contradict on a load-bearing point without explicit precedence/supersession.

Particularly:

```text
Pi ↔ Mastra Builder
Vercel AI SDK/custom loop ↔ Mastra Product Agent
agent_event ↔ OBS records
Group ↔ Workspace
tailnet ↔ company VPN
headless EVENT requirement ↔ SCHEDULE-only current realization
```

### F — Critical path / proof adequacy

Challenge whether 3K..3O are sufficient to get from this architecture to a buildable platform without another architecture mega-cycle.

Ask:

```text
Does first vertical actually prove the platform rather than only a dashboard?
Does 3L test the right tech assumptions?
Does 3M own all semantic recovery risk?
Will 3N test the real global contradictions?
Does 3O prove enough composition?
Does C-018 need a supersession index?
```

### Specific F-GPC-01 challenge

Resolve whether AGT-4 is:

```text
ALREADY RESOLVED
RECONCILE AS BUILDER/CONTROL-PLANE ASSISTANCE
DEFER WITH EXPLICIT C-003 AMENDMENT
REQUIRE DIFFERENT EXISTING OWNER
MATERIAL NEW SCOPE/OWNER
```

Do not add a new module/agent scope unless the requirement truly cannot be realized through an existing authority boundary.

---

## 17. Fable output contract

Append one review with:

```text
1. executive verdict
2. material findings only
3. C-001 pillar coverage verdict
4. C-003 requirement coverage exceptions
5. reference assimilation corrections
6. complexity/YAGNI cuts if any
7. supersession contradictions if any
8. remaining 3K..3O critical-path corrections if any
9. exact adjudication of F-GPC-01
10. whether a bounded 3A-R7 reconciliation is required
```

Final verdict must be one of:

```text
CURRENT STRUCTURE CONFIRMED — proceed to 3K
CURRENT STRUCTURE CONFIRMED + BOUNDED 3A-R7 REQUIRED BEFORE 3K
REOPEN SPECIFIC APPROVED AUTHORITY
STOP — product/architecture thesis materially inconsistent
```

Do not create authority, update C-* decisions, close/reopen phases or touch product code in the review commit.