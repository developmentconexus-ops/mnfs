# Conexus — registro de decisões

> **Role:** HISTORICAL / PROVENANCE DECISION INDEX
> **Not:** the current-state entrypoint or precedence-by-recency authority
> **Current questions:** [current/README.md](current/README.md) → [current/DECISION-RECONCILIATION.md](current/DECISION-RECONCILIATION.md) when an older decision appears inconsistent

Uma linha por decisão. Detalhe e evidência vivem no doc do tópico. Preserve este registro para história, rationale e provenance; não derive o target atual pela decisão mais recente/antiga sem o current reconciliation.

| ID | Data | Decisão | Doc |
|---|---|---|---|
| C-000 | 2026-08-10 | Conexus é produto novo, Mitra-shaped. MNFS kernel vira acervo de referência (REUSE plan-schema v2 + render visual + padrões store/erro; DEFER lease core; REJECT governança pesada). Programa ARR e replan M2 encerrados sem implementação. Produto nasce em repo próprio após tópicos 3+4. | [01](01-relacao-mnfs.md) |
| C-001 | 2026-08-10 | Visão ratificada: plataforma unificada AI-first sobre ERPs; Conexus Harness com princípios MNFS; fase 1 interna / fase 2 SaaS; caso 1 = replicar Analisador de Orçamentos (benchmark vs Mitra); pilares P1 cérebro / P2 engenharia no template / P3 AI-first (Mastra+ACP candidatos) + complementos C1–C4. | [02](02-visao-escopo.md) |
| C-002 | 2026-08-10 | Runtime do agente: hub soberano próprio (Node/TS + Postgres + pg-boss) orquestra workers **Pi** frescos via SDK pinado (interface `CodingWorkerRuntime` mínima, 1 adapter); Actor Pack compilado via ResourceLoader (AGENTS.md só se gerado pelo hub); tools TS nativas + MCP na borda; multi-modelo por papel (identidade runtime≠modelo persistida); gates humanos mecânicos; Claude Agent SDK = challenger sob trigger; Mastra fora do hub (minerar; OM candidata p/ tópico 15); auth SaaS baseline API/BYOK. Convergiu com pesquisa externa independente. | [04](04-runtime-agente.md) |
| C-003 | 2026-08-10 | Requisitos ratificados (revisão pós-C-002): MVP 36 / F1 17 / F2 2. Novos: HAR-10 forma de HUB (padrão Factory Missions — workers frescos + validador independente), HAR-11 multi-modelo por papel, QUA-4 Conexus Worker Eval. HAR-3 gate mecânico no hub; HAR-7 plano visual promovido a MVP; HAR-8/AGT-2 tool-agnósticos (refs Claude Code/Mastra removidas). | [03](03-requisitos.md) |
| C-004 | 2026-08-11 | Sandbox: fase 1a **100% local, US$ 0** — Topologia B (Pi no host) + SRT pinado (evidência AS-02 S1–S15 PASS); spike Vercel no Hobby grátis cedo (gates A1–A14); **gatilho de nuvem** (build com PC desligado) → PASS: hub Fly GRU + workers Vercel Pro ~US$ 23 / FAIL: Fly-total B+SRT ~US$ 8–12 (probe F1–F6); Modal challenger #2 por custo/latência (exigiria inference gateway); preview = Fly Machine GRU (`ActorRunSandbox` ≠ `PreviewEnvironment`); git push só no hub; normativos: Capability Gateway + `secretNotReadableByGuest`; `SandboxProvider` mínimo; SDKs pinados + conformance; removal conditions congeladas. Convergiu com 2ª rodada de pesquisa externa. | [05](05-sandbox.md) |
| C-005 | 2026-08-11 | Registro de artefatos: **git-first** (artefato = arquivo no repo, slug = nome, sem API de registro); versão dupla SHA + digest, registry guarda payload compilado imutável; registry único com kinds `query`/`action`/`job`/`integration` — kind escolhe executor e **role real de banco** (query read-only, action DML-only, migration role própria); sintaxe única `:param` → bind real via parser pinado + inputSchema obrigatório validado antes de executar; merge dispara **deployment atômico** (manifesto completo, migra→troca ponteiro por CAS, falha mantém anterior; rollback de ponteiro só sem migration, senão roll-forward); migrations timestamp imutáveis, DEV recriável (drift reprova no QA); SDK build (`runDdl/runDml/runQuery` só DEV + `executeCandidate`, via Capability Gateway) vs SDK runtime (`execute(slug,input)` + async/stop, deployment derivado server-side); envelope discriminado + serialização global; RC-1/2/3 congeladas. Validado: acervo Mitra + 2 pesquisas de mercado + Codex xhigh 2 rodadas (8,5/10) + corte anti-overengineering. | [06](06-registro-artefatos.md) |
| C-006 | 2026-08-11 | Camada de dados: **PostgreSQL único** (piso PG17, major pinado igual local/QA/cloud; motivo estrutural: DDL transacional + roles granulares — argumento LLM-dialeto descartado); 1 instância local WSL2, **1 database por projeto** + `hub_control`; roles mecânicas owner-NOLOGIN/migrator/query/action + ALTER DEFAULT PRIVILEGES; **3 QA gates** em database temporário do próprio cluster (rebuild de `template0` / golden fixtures determinísticas com asserção exata + provas negativas de privilégio / rehearsal com dump sanitizado — TEMPLATE só otimização); fase 1 **só migration transacional** ("gated deployment + cutover controlado"; janela `maintenance-required` = drain completo incl. jobs queued/deferred/retry, fail-closed); backup = `pg_dump -Fc` diário + pré-migration + manifesto de recuperação + **cópia cifrada B2 via rclone** + restore-test mensal do B2, PITR/pgBackRest só por gatilho de RPO; roll-forward first, sem time-travel local; ETL cursor+overlap+staging+upsert mantido + **política de deletes por entidade** e contrato de cursor obrigatórios (→ tópico 7); template congelado (`C.UTF-8` + ICU `pt-BR` em colunas de apresentação, timestamptz UTC, numeric, bigint identity, uuid app-side sem pin PG18, índice em FK, SQLFluff CI); invariante de audiência declarada + RC-1/RC-2 na ativação. Convergido: 2 pesquisas internas + deep research externa + Codex xhigh 2 rodadas (8,3→9,3/10). | [07](07-camada-dados.md) |
| C-007 | 2026-08-11 | Integração externa: conector = artefato git no kind `integration` com contrato declarativo **kernel estreito** `connector/v1` (identidade, auth shape, operações com effects[]/idempotency+UNKNOWN fail-closed/agentEligible/approvalFloor, extensões discriminadas p/ sync+paginação; ausência = não implementado); **sem runtime de hooks dia 1** (seam reservado, schema rejeita; gatilho = 1º provider fora da união fechada, hook computacional puro sem segredo, signing nunca vira hook livre); native-first (`sankhya.x.y`), unified models por gatilho, passthrough = dev/admin; dual-face no modelo (campos v1, tool projection/MCP adapter por gatilho); Connection no hub_control com `credential_backend`+`credential_ref` substituível + vault mínimo (crypto autenticada, chave fora do banco, key_version; AES-GCM pinado na implementação; broker OAuth = gatilho 1º OAuth **delegado**); testConnection obrigatório semântico na qualificação da Connection; webhook fora do v1 (extensão no 1º real); revisões imutáveis pinadas por projeto/ambiente, hosts = autoridade única (contrato → projeção compilada C-005); 3 gates separados (offline/Connection/promoção, canary removido); dispatch defer total; perfil Sankhya corrigido (client_credentials + X-Token sem refresh — reautenticação via `/authenticate`; ambientes sandbox×prod dia 1; authorization_meta). Convergido: acervo Mitra + 2 pesquisas internas + deep research externa + Codex xhigh 2 rodadas (6→8,5/10). | [08](08-integracao-externa.md) |
| C-008 | 2026-08-11 | Sandbox (supersede C-004): worker Pi ganha **máquina completa** — hub local + workers em **microVM Firecracker alugada (E2B, adapter único)** com root/apt/browser/portas; objetivo re-priorizado = agency (isolamento até sobe: kernel próprio × namespaces SRT); invariante desdobrado `durableSecretNotReadableByGuest` (ERP/vault/provisioning keys nunca no guest) + `guestReadableRunCapabilityIsEphemeralAndBounded` (chave LLM por run: hub cria, `expires_at` ≤ TTL, spend cap por ActorRun, fail-closed, sentinel scan, revogação+reconciliação); git **mediado pelo hub** (SYNC bundle sem credencial → WORK local → SHARE bundle+quarentena+validação; push só no hub, literal); egress deny-all + allowlist nativa E2B fora do guest (SRT rebaixado); `BuildValidationDatabase` sintético no sandbox (nunca "DEV"; autoridade continua local via Gateway); sessão 1h = orçamento ≤40–45min com marcos 45/50/55/58; `RunPreview` privado via proxy do hub (`PreviewEnvironment` adiado); ~US$100 crédito ≈ meses (custos ocultos reconhecidos); Daytona challenger por gatilho, Modal = nova decisão, exe.dev descartado, WSL2 = fallback não qualificado; gatilho "PC desligado" preservado em aberto; **ativação probe-gated `CX-SBX-E2B-01`** (11 itens, 1º build do Golden Path) com roteamento proporcional de falha e propriedades eliminatórias. PRESERVE: Capability Gateway, seam `SandboxProvider` mínimo, pinagem+conformance, removal conditions. Convergido: Codex xhigh 2 rodadas (6,5→9,1/10, "sem divergência arquitetural material"). | [05](05-sandbox.md) |
| C-009 | 2026-08-11 | **Sonda de manutencao (topico 16) fechada: a Mitra sustenta a segunda volta.** 14 turnos (V1, M1..M14), 76 observacoes. Veredito em 13 pontos, dos quais os estruturais: (1) o piso da plataforma e o pior comportamento possivel dentro dela — cobertura declarada, aposentadoria de artefato e recusa de inventar dado apareceram como escolha do modelo, nunca como invariante; as unicas 3 invariantes que reprovaram algo vieram do ecossistema (`tsc`, `--noUnusedLocals`, FK); (2) o Conexus nao precisa de modelo melhor, precisa tornar **obrigatorio** o que o bom modelo fez espontaneamente; (3) manutencao so e corrigida **por classe** quando uma invariante mecanica reprova; (4) artefato orfao e detector de garantia quebrada — registro guarda leitores/escritores, "escrita sem leitura" vira alerta continuo (maior retorno por custo da sonda); (5) consolidacao != extensibilidade — duas alavancas distintas; (6) o erro que atravessa tudo e **verificar o canal em vez do conteudo**, 8 camadas independentes numa noite, nenhuma pega por build/teste/diff; (9) **o teto da Mitra nao e de codigo, e de expressividade da camada de conexao** — sem conector OAuth2, sem `updateIntegrationMitra`, credencial modelada como config estatica e nunca como estado vivo (3 ocorrencias); so aparece quando a segunda volta encosta no mundo externo; (10) o teto e contornavel por `fetch` cru da SF **sem allowlist de egresso** — resolve a integracao e ao mesmo tempo mostra que o guarda de credencial nao e fronteira: o Conexus precisa de credencial viva **e** egresso governado; (11) falso positivo silencioso reaparece **nas proprias iscas e barreiras** — isca precisa ter a forma do caso real, barreira ancora em egresso (o que o sistema faz) e nao em destino (valor que a refatoracao move); (12) na dimensao mais dificil, resposta favoravel — o agente achou que a propria trava deixava passar 2 renovacoes de um `refresh_token` de uso unico e consertou **antes** de executar a lista pedida, evitando destruir a conexao real do usuario; (13) limite operacional medido: 3 paradas por limite de sessao num dia, janela de 5h, 2 delas com a mensagem consumida com `out: 0`. PRESERVE dos pontos 7-8: a Mitra **nao** e ingenua na camada de dado (guarda contra placeholder em posicao estrutural, `DROP` bloqueado por padrao) — falta-lhe **ambiente**, nao guarda; e o agente recusou o contorno interno do guarda mesmo com autorizacao humana e caminho tecnico conhecido. Alimenta T5, T6, T7, T8, T11, T13, T14. | [16](16-sonda-manutencao-mitra.md) |
| C-010 | 2026-08-11 | Agente de 1ª classe: agente de produção = **artefato declarativo git-first** (kind `agent`, contrato `agent/v1`) no registro C-005 — manifesto YAML (modelo+prompt+tools pinadas+policies+budget) é a **fundação do construtor de agentes** (AGT-2/C-001); runtime = **loop leve no hub** com **Vercel AI SDK** como camada de dialeto multi-provider dia 1 (major pinado; emenda do ratificador: multi-provider é objetivo de produto), dentro da interface própria `ModelProviderAdapter` (budget pré-chamada/custo no trace/alias-drift/taint são nossos; vendor-específico via providerOptions), sem framework de AGENTE (Mastra rejeitada como fundação; OM candidata no T15) e sem sandbox; **só 4 objetos novos** (AgentDefinition, Conversation, AgentRun, ApprovalRequest — deployment via manifesto C-005 estendido + atestação de eval separada; sem role/Connection por agente, `actorType=AGENT`+actorId estável); ToolProjection compilada fail-closed (agentEligible default false, override só aperta, 4–8 tools alvo/20 teto, conformance tests) sobre metadata de segurança **no contrato do artefato** → **RC-2 do C-005 acionada**; HITL mecânico: AWAITING_APPROVAL durável → claim atômico → executor determinístico do envelope exato (hash total, cifrado) → modelo só redige do receipt; precondição tri-estado (guarda no efeito / NONE justificado / BEST_EFFORT declarado), idempotency key por efeito, OUTCOME_UNKNOWN+reconciliação; leitura só por queries registradas (PROJECT_WIDE declarado); budgets por **unidade** pré-efeito (8 campos, cardinalidade desconhecida fail-closed); contexto em camadas provider-agnóstico + trustClass + taint latched; trace estruturado + captura raw opt-in cifrada TTL; modelo = mais barato que passa golden eval com margem (Haiku×Sonnet), pinado, drift de alias invalida; renderer sanitizado (anti-exfil markdown); headless = semântica no MVP, executor F1 (AGT-3 só fecha com consumidor real; 4 casos nomeados); **escopo MVP explicitamente mais estreito que a Mitra** — tool analítica tipada = gatilho ligado ao T15. Ajustes: C-002 (T9 ≠ ActorRuns Pi), C-005 (kind novo + invariante "compilação+lifecycle+handler"), C-007 (EXTERNAL_SEND conta em ambos budgets; tool projection acionada). Convergido: acervo Mitra + pesquisa interna + deep research externa (3 preços corrigidos) + Codex xhigh 3 rodadas (7,3→8,9/10; r3 alinha emenda AI SDK com 7 condições pré-deploy — lockfile frozen/SBOM, tipos da lib não vazam, providerOptions allowlist, AI Gateway+telemetria off, conformance ao vivo, sem effectful paralelas, Batches fora da abstração comum). | [09](09-agente-primeira-classe.md) |
| C-011 | 2026-08-11 | Cérebro da empresa: **pacote de conhecimento git-first por GRUPO** em 3 eixos (SEMANTIC/KNOWLEDGE/EVIDENCE) — kind `brain` (`brain/v1`) + `brain-binding/v1` no registro C-005; separação lógico×físico×operacional (BrainDefinition grupo / ProjectBrainBinding projeto com prova de conformidade / BrainPack compilado imutável = camada empresa do ContextPack C-010; trace com 4 digests); fonte git de escopo GRUPO (nunca no repo do 1º projeto), 3 autoridades (git=publicado, Postgres=operacional proposals/health, registry=serving), health fora do digest; YAML MetricFlow/Ossie-aligned + bloco LLM Cortex + markdown frontmatter tipado (DSL pequena própria assumida; Ossie ex-OSI = REFERENCE); 3 classes de regra (número→metric estruturada, fórmula compila via lifecycle C-005/C-006; interpretativa→caveat; processo→knowledge); entrega inline determinística com budgets DUROS no deployment (maxBrainTokens, compilação falha; teto dezenas de regras IFScale; dependency closure — métrica nunca sem caveat) + RAG só por G4 mensurável; herança: pin por digest, publish AVAILABLE ≠ promote (UPDATE_AVAILABLE informativo, revalidation na promoção), override explícito `overrideOf`+reason, conflito não declarado = falha de compilação, `final` no pai, binding≠refinement≠override, merge por tipo de item; **AnalyticQuery = 2º regime de leitura** (emenda C-010 comp.7) — subset Conexus inspirado em Cube, só IDs do Brain, AST restrita + prova SELECT-only + role read-only, limites min(request,caps,budget) default dezenas/centenas, single-dataset v0, many_to_many rejeitado, fonte componível `analyticSource:true`; discovery F1 sonda **TDD*-first** (dicionário nativo Sankhya; profiling direcionado = auditoria; badge de proveniência; probe CX-BRAIN-DISCOVERY-01 antes de CER-3); retroalimentação F1 = KnowledgeProposal com gate humano, conflito por ID+escopo+footprint, NO AUTO MERGE, self-write REJECT (anti-MINJA); drift: 5 estados (UNVERIFIED/VALID/SUSPECT/INVALID/CHECK_ERROR) que o runtime respeita, SUSPECT effectful bloqueia, health append-only + reducer versionado, recheck de health antes de resposta/efeito; segurança: PII lint + sampleSource, custom_instructions fechado, authority lattice (Brain nunca amplia autoridade); golden eval = perguntas de negócio; v0 = 1 grupo caso 1 dirigido por golden questions, 4 fundações não-retrofitáveis (namespace/digest+pin/provenance/draft-publicado); gatilhos G1-G7; **probe bloqueante CX-BRAIN-V0-01** (12 itens) antes do 1º deploy Brain-backed. Diferencial = COMPOSIÇÃO cross-project governada, prova = 2º projeto nasce sabendo (G1). Convergido: acervo Mitra + pesquisa interna (4 varreduras) + deep research externa + Codex xhigh 2 rodadas (7,9→8,8/10). | [15](15-cerebro-empresa.md) |
| C-012 | 2026-08-11 | Scaffold + frontend: **scaffold versionado e reproduzível, rico em infraestrutura e pobre em features** — template decide engenharia, agente decide aplicação. Identidade: `scaffold.manifest.json` dia 1 (digest scaffold + registry + runtimeContract), mas manifesto não prova a própria origem — atestação = `scaffoldManifestDigest` **calculado pelo hub**, adulteração reprova; migration ledger git-first. Ownership **3 classes** (GENERATED nunca editar + `git diff --exit-code`; PLATFORM-CONTRACT muda só por migration kit; APP-OWNED do projeto) com classificação TOTAL e segurança crítica **não-excepcionável** (renderer/sanitizer chat, anti-exfil, card aprovação, runtime client, identidade fake em prod); update só por **migration kit explícito** (from/toDigest, pre/postconditions, idempotência, fluxo C-008; propagação silenciosa PROIBIDA; `scaffoldException` rastreável). Stack: React+TS strict ampliado+Vite SPA (Next e TanStack Start REJECT MVP), TanStack Router file-based (routeTree GENERATED, search params tipados), estado em 3 casas (Query/URL/React; state-manager e form framework DEFER com gatilhos), tabela TanStack headless `<table>` paginada server-side (virtualização só após medida de DOM; URL não concede capacidade). UI-kit: **copy-in via Registry Conexus servido pelo hub** (formato shadcn CLI+MCP, CLI/schema pinados, item por digest, anti-traversal, lifecycle scripts proibidos p/ deps de registry e allowlisted por digest na closure da plataforma; `@conexus/ui` npm REJECT; prontidão do kit = condição de probe); enforcement 4 camadas (registry/MCP, noRestrictedImports, type contracts, product invariants; HTML nativo não banido). **Emenda C-005**: gatilho acionado — artefato exposto ao frontend exige `outputSchema` como AUTORIDADE RUNTIME no Gateway (representação serializada, subset JSON Schema versionado, additionalProperties:false, `OUTPUT_CONTRACT_VIOLATION`); `dataMeta` condicionalmente obrigatório no braço success (`ReadDataMeta` provenance/sourceAsOf/retrievedAt/rowCount pós-shaping/coverage união fechada × `ActionReceiptMeta` receiptId/outcome/effectUnits; success+outcome:FAILED = receipt de falha, não efeito ok); ponteiro CAS ÚNICO passa a selecionar também `frontendDistDigest`; codegen fino direto do registro (OpenAPI intermediário REJECT). Handshake: 3 digests sem ciclo; `runtimeContractDigest` **estrutural SEM health** (emenda C-011 — health segue gate server-side via `CAPABILITY_UNAVAILABLE_HEALTH`, 4 digests do trace intactos); mismatch = `CLIENT_OUTDATED` fail-closed. Honest UI mecanizada: `RequestState<T>` × DataMeta ortogonais, provenance POR TIPO, DataQualityBoundary/SampleBanner automático em partial, EmptyState.reason obrigatório, unknown≠0, "rótulo não exagera" = golden product-invariant tests (nunca lint semântico). Gates: verify = generated-check→tsc→Biome→ESLint residual (só @tanstack/eslint-plugin-query dia 1; oxlint DEFER; nunca 3 linters)→Knip→invariants→testes→build→Playwright→ARIA→bundle calibrado; HUB decide per-turn × before-delivery; boundary antes do 1º deploy (codegen REPRODUZIDO no hub, teste negativo effectful server-side, prod falha com LOCAL_DEVELOPMENT_IDENTITY, 404 de asset, axe, CSP+headers). Chat embarcado = fronteira PLATFORM-CONTRACT não-excepcionável (raw HTML off, anti-exfil separada da sanitização — C-010). Publish F1: hub serve dist DIRETO (sem Caddy, gatilho nomeado), `deployments/<project>/<digest>`, troca atômica de ponteiro, headers desde o 1º deploy, **CSP posição 2** (script-src estrito; style-src-attr = concessão isolada p/ valores numéricos tipados, nunca strings CSS/URLs de usuário/ERP); preview = proxy autenticado do hub; cloud futuro Cloudflare Workers static assets (Vercel Hobby REJECT). Auth seam: SessionBoundary/ViewerContext/cookie HttpOnly do hub, zero token no browser, frontend RBAC nunca autorização (provider = T12). Faseamento empírico: scaffold v0 → caso 1 GERADO → falhas classificadas por classe → v0.1; lista NÃO-construir com gatilhos nomeados; HAR-5 reinterpretado (byte-controle = nascimento reproduzível + autoridade sobre GENERATED/PLATFORM-CONTRACT). **Probe bloqueante CX-SCAFFOLD-V0-01 (12 itens)** antes do 1º deploy — nenhuma garantia arquitetural vive só no probe. Convergido: acervo Mitra + design system extraído do Marketplace Central + pesquisa interna (4 varreduras) + deep research externa + Codex xhigh 3 rodadas (7,9→8,4→8,8/10). | [10](10-scaffold-frontend.md) |
| C-013 | 2026-08-12 | Observabilidade mínima: **eventos estruturados append-only no Postgres do hub** — telemetria diz o que aconteceu, NUNCA decide autorização/aceite; **5 camadas de evidência** (Hub autoridade / runtime Pi do builder / sandbox E2B / app em teste / artefato publicado) com timeline causal correlacionável. Tabela única `agent_event` (hot fields relacionais + payload jsonb; sem GIN/partição dia 1 — gatilhos por latência/vacuum medidos), ingestão idempotente (`producer_instance_id`+`producer_event_id`+`source_seq`), INSERT-only, `occurred_at`≠`recorded_at`, causalidade por `parent_event_id`, buffer bounded com `events_dropped` coalescido. **`producer_trust`** (HUB_AUTHORITY/GATEWAY_AUTHORITY/PROVIDER_OBSERVED/GUEST_OBSERVED) — acceptance só consome os 2 primeiros; ingestão do sandbox via capability efêmera, guest nunca fornece ids autoritativos. Schema Conexus soberano + OTel `gen_ai.*` como **mapping versionado** (projeção, nunca ontologia). Dois regimes: WorkerTurn = telemetria NATIVA Pi 0.84.1 via `TelemetryContext` injetado (nunca stdout; supervisor emite MISSING; `pi.*` = diagnóstico, nunca contrato) × ConversationTurn = nosso loop (C-010 intacto, AI SDK telemetry off); fim de turno pela fonte durável. Conteúdo: nunca truncado SILENCIOSAMENTE — `safe_projection+content_digest+original_byte_length+content_state` (CAPTURED/OMITTED_BY_POLICY/TOO_LARGE/UNAVAILABLE). Custo: `usage_state`×`calculation_state`(incl. MISSING_PRICE)×`reconciliation_state` independentes, 3 colunas que nunca se sobrescrevem, preço pinado por versão git (seed LiteLLM), histórico nunca recalculado, cache r/w separado, custo sandbox separado, effect units = contabilidade operacional (custo monetário DEFER), rollup=GROUP BY USD-only, UI rodapé tokens+USD+duração; desconhecido≠0. **Persist-first + admission ledger** substitui preflight universal (reinterpretação C-009 p.13): mensagem persistida antes de chamada, reserva atômica, `blocked_until` por sinais observados, máquina de attempt PENDING/PENDING_CAPACITY→RESERVED→DISPATCHED→COMPLETED/FAILED/OUTCOME_UNKNOWN/CANCELLED — OUTCOME_UNKNOWN nunca tem retry automático, settlement por terminal, mensagem nunca consumida silenciosamente. Checklist vivo: worker propõe → HUB aplica → Postgres autoridade → UI projeção; `expectedRevision` CAS; morte do worker ⇒ INTERRUPTED; narração = parâmetro obrigatório de tool; AG-UI valida forma, não adotado. **tasks.md = memória de propósito** (OBS-77.1), não autoridade operacional; gate mecânico no SHARE via bloco estruturado planRevision+itemId/statusCode contra Postgres (prosa nunca interpretada). Escada WORK_COMPLETED→RESULT_PERSISTED→VERIFIED→DEPLOYED→**SERVED_VERIFIED** (GET + digest servido==esperado; SEM smoke pós-CAS — RC-3 C-005 preservada; hub fecha, nunca o agente). **Emenda C-012**: outcome ganha PARTIAL + breakdown multiunidade (total=succeeded+rejected+unprocessed+unknown; attempted exclui unprocessed; OUTCOME_UNKNOWN precedência máxima; atômica nunca PARTIAL). **Emenda C-005**: compilador emite directReads/Writes + resolvedReads/Writes + `resolutionState`; lineage declarada×observada (Gateway emite edges) com taxonomia órfão estrutural / UNOBSERVED_ACTIVE / escrita-sem-leitura (warning de alto valor, supressões legítimas). Builder evolui TEST→BUILD→**RUN→OBSERVE→ASSERT**→SHARE: Spotlight Sidecar DENTRO do E2B (loopback only, sem auth ⇒ nunca RunPreview/público/prod com teste negativo, DSN off, PII off, GUEST_OBSERVED por construção, jamais prova de aceite; hub persiste só resumo `runtime_validation` bounded) via spike CX-OBS-SPOTLIGHT-01; E2B = resumo por run (metrics_state COMPLETE/PARTIAL/MISSING, percentil NULL sob mínimo de amostras, teardown solicitado×observado; amostras 5s não importadas). Superfície F1 = **Run Timeline causal** (não dashboards) + tela de status espelhando as 4 máquinas congeladas sem reinterpretar + alarme in-app (externo = gatilho C-007) + log filtrável com export JSONL derivado. Retenção dia 1: 90d + disk high-water mark, GC por run completo auditável, ledgers de domínio FORA do GC; fronteira dura: Postgres caído ⇒ domínio PARA fail-closed; falha de TELEMETRIA com Postgres saudável nunca altera execução; evidência obrigatória de acceptance enumerada (HUB/GATEWAY + verifier). Redaction por campo com allowlist (OBS-19). NÃO-construir com gatilhos: Langfuse/exporter, dashboards, alerting externo, sampling, evals CI, warehouse, partição, AG-UI, envelopes Sentry, query builder, conversão de moeda. **Probe bloqueante CX-OBS-V0-01 (9 itens: conformance/dedup, correlação e2e, anti-forja, adapter Pi, golden de custos, máquina checklist, tabela-verdade receipt, redaction/GC, fronteira telemetria×Postgres)**; dobras: E2B no CX-SBX-E2B-01, revision proof no CX-SCAFFOLD-V0-01. Convergido: pesquisa interna (3 varreduras) + deep research externa (incl. Q11 Agent Runtime Observability) + Codex xhigh 4 rodadas (7,7→8,4→8,3→8,8/10). | [11](11-observabilidade.md) |
| C-014 | 2026-08-12 | Ciclo de vida F1: ReleaseManifest imutavel como composition root sob o ponteiro CAS unico (releaseManifestDigest == deploymentManifestDigest; consolida extensoes C-010/C-011/C-012; fechamento em ordem total anti-ciclo; ReleaseRecord = projecao, PromotionRecord = historico append-only); regra de ouro: nenhum caminho de mudanca de schema/artefato/config fora do release; git model: 1 commit canonico por Work Unit (nunca model turn), branch por Change (change/CX-id) conduzida por 1 construtor, usuario ve Releases; repo por app, GitHub privado, hub unico portador de credencial; ambientes F1 = {BuildValidationDatabase, workspace DEV, RunPreview efemero por digest, PROD lazy no primeiro promote = database separado no mesmo cluster isolado por roles (emenda C-006 comp.2), CIC-2 reinterpretado como ambiente isolado com lineage}; binding ambiente-app x Connection-environment explicito; EnvironmentConformance mede alvo real (roles/grants/rolbypassrls, ledger x actual, schemaFingerprint versionado catalogo-apenas, Connection revision pinada == ativa, digest servido) com DRIFT => STOP; promote = transacao de dominio em dois planos (candidate BUILDING->VERIFIED->AVAILABLE; promotion APPROVED->...->POINTER_SWAPPED->SERVED_VERIFIED) com gate humano F1, nunca rebuild, CAS_CONFLICT por expectedGeneration, retry idempotente, SERVE_VERIFICATION_FAILED, previousReleaseId null explicito, steps como agent_event correlacionados por promotionId (emenda C-013 par.3); config em duas identidades: configContractDigest na identidade do release x material secreto versionado fora (rotacao sem release quando mesma semantica), lint anti-import.meta.env (emenda C-012 par.14); migrations: QA-DB-1/2/3 universal, dois ramos - backward-compatible com prova N-1 mecanica (emenda C-005 comp.8) e janela recuperavel, maintenance-required com drain + MAINTENANCE_RECOVERY_REQUIRED (serving antigo bloqueado, saida so por CAS/restore/forward-fix); producao forward-only, PITR = disaster recovery, pg_dump diario, drift check periodico; rollback = re-point pelo mesmo gate com elegibilidade mecanica (ROLLBACK_UNAVAILABLE_SCHEMA_INCOMPATIBLE), reverte composicao inteira com revalidacao leve de pins, UI explicita que nao restaura dados, tag = rotulo que resolve a manifesto arquivado (emenda C-005 comp.12); escada C-013 por ambiente-alvo (emenda par.13); provenance = digests + Postgres sem PKI; glossario anti-colisao de migrations/ledgers; T-7 nao-unificacao dos 4 mecanismos de pin; CIC-4: duplicacao sem dados/credenciais; probe bloqueante CX-REL-V0-01 (15 itens); NAO construir: staging, release train, canary, blue/green, feature flags, DB branching, multi-region, signing, engine de down-migrations. Convergencia adversarial 7,8->8,4->8,8 | [12](12-ciclo-de-vida.md) |
| C-015 | 2026-08-12 | Runtime publicado F1: conta central no hub_control (account_id UUID estavel, uma conta -> N apps; setup credential de uso unico + troca obrigatoria; sem self-signup) + ProjectMembership explicito (conta x projeto -> role; membership checada ANTES de role; ausente = 404 indistinguivel; projectId/appId sempre derivados server-side, payload adulterado ignorado); credencial email/senha NIST 800-63-4 (>=15 chars, sem composicao/rotacao forcada, blocklist) + Argon2id versionado; sessao server-side em Postgres (so hash, idle+absolute expiry, auth_version) com cookie opaco HttpOnly SameSite=Strict (prefixo __Host- quando TLS), rotacao em login/reset/privilegio, DISABLE/reset revoga TODAS as sessoes, zero JWT browser-side e zero token em URL/localStorage (anti-S9); bind F1 = loopback apenas, tailnet = opt-in explicito com banner e agent_event, bind publico sem TLS => hub recusa subir fail-closed; trust zone declarada: N apps na mesma origem = UMA zona de confianca de codigo browser (Path nao isola; CSP reduz probabilidade, nao blast radius) com gatilhos de subdominio ampliados (JS de terceiros, classes de confianca distintas, app publico/embed, requisito de isolamento); RBAC: roles fechadas {admin, member} (agente NAO inventa role), ViewerContext fechado {userId, displayName, roles, capabilities} com capabilities DERIVADAS server-side (C-012 intacta), home por perfil = presentation policy, trace so operador (delegacao C-013 par.19); manifesto de permissoes fail-closed: action sem allowed_roles = MANIFEST_INVALID => candidate REJECTED (NUNCA default admin), admin_only explicito, allowed_roles camada ADITIVA sobre effects/approvalFloor/agentEligible do registro (nunca substitui/rebaixa; conflito = erro de compilacao), teste positivo+negativo por action, policy amarrada ao releaseManifestDigest; permission diff no promote (melhor-que-mercado): widening (role adicionada, admin_only->member, READ->WRITE, reducao de approvalFloor, agentEligible novo) = evidencia OBRIGATORIA do gate humano C-014 (promote sem diff = bloqueado), narrowing anotado; authority x audience separadas: F1 uma audiencia (C-006 comp.11 intacta), filtro por identidade = WHERE parametrizado com valor injetado pelo hub caso a caso, particionamento sistematico = RC-1, RLS futuro = defesa adicional nunca substituta; enforcement unico no hub/Gateway (execute checa sessao -> membership -> roles -> policy; mudanca de permissao NUNCA gera CLIENT_OUTDATED; sessao expirada = SessionBoundary.anonymous; administracao = rotas de plataforma do hub, nunca SDK runtime do app); embed NAO construir F1 (frame-ancestors self; gatilho: consumidor nomeado + TLS + origens exatas; desenho registrado: codigo de troca de USO UNICO ~60s -> cookie __Host- Partitioned -> 303 limpa URL -> postMessage targetOrigin exato; nunca bearer duravel em URL; PUB-4 = chat do proprio app, piso Mitra menos S3); rota publica NAO existe F1 (usuario externo = RC-1; desenho registrado: dupla declaracao classe de acesso da rota AUTHENTICATED|PUBLIC_FORM|PUBLIC_TOKEN x publicEligible default false, compilador reprova rota publica -> capability nao publica; flag app publico REJEITADA); storage PUB-5: blobs CAS SHA-256 no filesystem do hub com dedup global, acesso SO por attachment_id UUID opaco (digest nunca e rota/credencial, existencia nunca confirmada por digest), autoridade de attachment INTEIRA no hub_control (mesmo database do registro de blobs state/refcount/generation => transacao ACID unica; projeto guarda so id opaco como dado; projecao de leitura = gatilho), pipeline com reserva atomica de capacidade pre-stream (tamanho declarado obrigatorio, fail-closed; chunked sem tamanho = nao suportado), magic bytes + allowlist + limites por classe (XML: DTD/XXE off, bytes originais preservados; archive nao suportado), PENDING->AVAILABLE por CAS com lease + reconciler, GC em duas fases (quarentena via CAS por geracao; reativacao atomica no commit de referencia; remocao fisica so apos janela > intervalo de backup com re-verificacao), higiene de nome RFC 6266 (nome fisico = digest), download so por rota autenticada (Content-Disposition attachment + nosniff; html/svg nunca inline), limites sistemicos obrigatorios (low-disk watermark fail-closed, metricas C-013) sem quota comercial; backup mecanizado (emenda C-006 comp.7): lock de GC -> dump hub_control PRIMEIRO -> dumps de projetos enumerados do dump -> blobs + manifesto de integridade (digest/size/cardinalidade/checksums), restore-test valida manifesto INTEIRO + hub_control restaurado como indice + id orfao => indisponivel nunca 500; fiscal: Sankhya = arquivo fiscal AUTORITATIVO, Conexus = cache NON_AUTHORITATIVE com chave/protocolo, retention_class FISCAL_NFE reservada (CAS prova integridade de bytes, nao autenticidade fiscal); emendas: C-003 PUB-2 reescrito (BFF same-origin cookie; origem separada volta como piso com origem propria), C-003 PUB-5 reescrito (parte publica -> gatilho de ingress), C-006 comp.7 (backup blobs), C-012 par.17 (shape ViewerContext), C-014 (permission diff no gate), PUB-3 reinterpretado (SDK administrativo do hub); NAO construir F1 com gatilhos: self-signup, SMTP/magic link, passkeys, SSO/SCIM, subdominio por app, RLS, field-level, embed, rota publica, ClamAV, object storage, quota comercial, versionamento de anexos, policy engine generica; probe bloqueante CX-PUB-V0-01 (20 itens a-t incl. cross-projeto, bind sem TLS, restore-test). Convergencia adversarial 8,2->8,4->8,4->8,1->8,7 | [13](13-runtime-publicado.md) |
| C-016 | 2026-08-12 | Seguranca proporcional F1 (escopo nominal ja ~85-90% congelado por C-005..C-015; todas as 9 falhas S da Mitra respondidas; SEG-4 e a decisao-mae): supply chain do app gerado = gate de admissao de dependencia (install deterministico com frozen lockfile + integridade fail-closed; quarentena temporal ref. 7 dias com lane de excecao para security update - KEV/malware confirmado/CVE critica direta alcancavel/falha de integridade do registry; pacote fora do catalogo = DEPENDENCY_PROPOSAL com aprovacao humana no gate de promote = dependency diff, emenda C-014, irmao do permission diff; catalogo por capability nunca lista numerica, conteudo nao ratificado; autoridade de install script EXCLUSIVA do catalogo pinado pelo hub por package@version+digest, metadata do app nao concede script, install fail-closed em quarentena E2B sem credencial; scan OSV no diff e no release, achado nao-critico = Finding com prazo nunca bloqueio automatico); patch do hub: Renovate semanal agrupado, automerge so conjunto de baixo risco DECLARADO (patch nao concede automerge por si), deps sensiveis (auth/crypto/banco/sandbox/pm/Gateway) sempre revisao humana, security update fura quarentena, major manual; egresso em DUAS superficies: servidor Gateway-only por construcao (artefato sem primitiva de rede; probe cobre fetch/http/net/tls/dns/WebSocket/child_process/native addon; destino+redirect validados contra origem exata da Connection; sem DNS pinning novo - gatilho) + browser self-only por CSP em todas as diretivas de carga/envio restringindo ORIGENS DE REDE (fecha C-012 par.16 como invariante de egresso; preserva concessao style-src-attr; prova = teste negativo no navegador); EMENDA C-015 par.5: HTTP so em loopback, acesso browser via tailnet = HTTPS obrigatorio (cert .ts.net; WireGuard cifra transporte mas browser ve HTTP - __Host-/Secure/secure context seriam fisicamente falsos), tailnet HTTP deixa de ser estado normal; credencial viva (fecha delegacao C-007): entrada SO por rota de plataforma write-only com selecao explicita de conexao+ambiente (chat/agente NUNCA recebem segredo - agente gera link; anti-S4/OBS-02/03), blast radius descoberto DA API no testConnection nunca so da descricao, rotacao de refresh token rotativo = DESENHO REGISTRADO com gatilho (Sankhya F1 = client credentials; quando ativado: maquina READY->ROTATING->{READY|REAUTH_REQUIRED|OUTCOME_UNKNOWN}, attempt_id, lease com expiracao, CAS por rowsAffected, transacao nunca aberta durante rede, key_version != refresh_generation, OUTCOME_UNKNOWN sem retry automatico e desfecho honesto pode ser REAUTH_REQUIRED); erro sanitizado invariante unico: detalhe interno nunca atravessa o Gateway (erro tipado + correlation ID; trace so em log redacted server-side), envelope de integracao com traffic_state {NOT_SENT|SENT_NO_RESPONSE|RESPONSE_RECEIVED}, status bruto so com RESPONSE_RECEIVED, apresentacao PROIBIDA de nomear ator externo sem RESPONSE_RECEIVED (anti-OBS-72.7); compilador fecha residuos pos-bind de uma vez: LIKE escapado por default com opt-in de wildcard, ORDER BY so por allowlist de colunas declarada, teto server-side de paginacao; rate limit de execute() por conta x classe (READ_CHEAP|READ_HEAVY|WRITE_LOCAL|EXTERNAL_EFFECT|EXPORT; valores nao ratificados) com armazenamento por criticidade: EXTERNAL_EFFECT/EXPORT = contador duravel no Postgres (familia do admission ledger C-013/budgets C-009; restart nao zera limite de efeito), READ_* = memoria, WRITE_LOCAL duravel quando approvalFloor > NONE; LGPD F1: ROPA gerado do catalogo de sync = DRAFT_FROM_TECHNICAL_CATALOG (campos juridicos exigem decisao humana), minimizacao no sync (coluna pessoal sem consumidor nomeado nao e espelhada), exclusao por classe DELETE_ALLOWED|RETENTION_BLOCKED_LEGAL|ANONYMIZE_ALLOWED (retencao fiscal do dado-fonte nao justifica retencao indefinida de projecoes/caches), RIPD/DPO = F2 por gatilho; forma = Conexus F1 Security Baseline com 16 propriedades testaveis (invariante -> fronteira fisica -> probe positivo+negativo -> referencia ASVS 5.0 L1/AISVS 1.0/ANPD com gaps declarados; nunca "ASVS compliant"; item 16 = capability efemera do sandbox formalizada de C-008; probes novos so onde nao ha cobertura), threat model por excecao = Four Questions de Shostack por gatilho nomeado, permission diff = detector continuo SO da autorizacao (declarado nao cobrir supply chain/XSS/segredo/disponibilidade); NAO construir F1 com gatilhos: Socket/Snyk, dependency firewall proprio, Redis para rate limit, firewall de egresso de runtime, OPA/Cedar/OpenFGA, threat model formal recorrente, SIEM/SOC2, Vault/KMS externo, SAST/DAST enterprise, automacao RIPD completa, maquina de rotacao de refresh token, DNS pinning no Gateway. Convergencia adversarial 8,2->8,8 | [14](14-seguranca-proporcional.md) |
| C-017 | 2026-08-12 | Modelo de engenharia + execucao agentic (ultimo topico de planejamento; T10 fechado sem decisao — C-017 atribuido ao T17): principio central = Hub controla identidade/autoridade/boundaries/budgets/Evidence/gates/estados terminais, agente mantem tatica local; regra anti-overengineering interna (entidade so se muda decisao mecanica, preserva fato ou elimina classe de falha nomeada). Hierarquia = work graph do BUILDER Group->Project->Change->WorkUnit->ActorRun (runtime de producao e release = grafos separados, IDs comuns, correlacao via agent_event); Mission/Milestone NAO existem F1 (gatilhos); Evidence = projecao, Handoff = artefato JSON tipado. Correctness ANTES de decompor: assertions verificaveis com ID dentro do Change (oracle com fonte autoritativa, witness anti-vacuidade, caso negativo, UNVERIFIED/BLOCKED distintos de FAIL, manual => gate humano), answerability operacional (required_access = pre-condicao declarativa, nunca concede capability), NO_CHANGE_REQUIRED valido via Work Unit de verificacao sem mutacao, fulfills[] sem orfaos, contrato ATIVO pinado por contractRevisionDigest com autoridade no hub/Postgres (worker recebe copia digest-pinada no Actor Pack, nunca altera revisao aprovada), revisao SEMANTICA => HANDOFF_REQUIRED + novo checkpoint + Evidence anterior STALE. Checkpoint humano em TODO Change (HAR-3 intacta; proporcionalidade regula PROFUNDIDADE, nao existencia); discovery via Gateway antes do contrato quando toca dado real (HAR-2); plano visual aprovado pinado por digest (HAR-7/HAR-4). Validacao em camadas: mecanico -> testes -> RUN/OBSERVE/ASSERT -> fluxo no RunPreview servido PRE-promocao (RC-3 intacta: pos-CAS so GET+digest) -> validador LLM fresco SO quando material -> humano; validador = Pi fresco em E2B sem write tools, identidade/Connection read-only com sink de efeitos desabilitado + teste negativo de mutacao, ActorRun proprio sob budgets C-009 com orcamento reservado pelo Change, reporta Finding nunca conserta; validator_report != hub_verified_evidence (aceite so HUB/GATEWAY_AUTHORITY apos verificacao mecanica ou decisao humana; parecer LLM sem oracle executavel => rota humana); aceite exige matriz assertion x verdict COMPLETA (UNVERIFIED em MUST bloqueia; nenhum Finding nunca e prova); assertion effectful: dry-run/test Connection ou sink deterministico com receipt, nunca PASS por inspecao de UI; EMENDA C-003/HAR-11: validador de provedor diferente rebaixado a gatilho experimental (independencia de CONTEXTO e o que importa; zero evidencia controlada de ganho por provedor). Finding unico duravel: eventos finding.* HUB_AUTHORITY + projecao Postgres (padrao checklist C-013), fingerprint canonico versionado, transicoes CAS, reabertura por parent_finding_id; tabela deterministica severity x contract_impact x reversibilidade x autoridade -> rota (local/fix-unit/replan/humano), hub so ELEVA; cap por conteudo (mesmo fingerprint sem hipotese nova => elevacao; ref 2 ciclos locais nao ratificada) + orcamento agregado de correcao no admission ledger; stuck != complete. RigorProfile CALCULADO (contribuicao propria, sem precedente publico): piso = max(efeitos declarados, sinais mecanicos do diff, ambiente-alvo), tabela normativa FAST<BOUNDED<CONTROLLED, fail-closed (desconhecido NUNCA reduz; sem sinal determinavel = nunca FAST), detector unico versionado recalculado em dispatch/fechamento/composicao, agente nunca rebaixa. Actor Pack simetrico ao ContextPack/BrainPack: standards[] com digest de fonte PUBLICADA pelo hub (nunca do change julgado), obrigatorio = eager, falha de compilacao BLOQUEIA dispatch (fecha #118 estrutural); frescor semantico = review triggers + humano, sem IA de frescura. Politica fora do change julgado: snapshot pinado por digest no dispatch (anti-TOCTOU), mudanca semantica bloqueia dispatch E aceite ate novo checkpoint; fix por RETENCAO de tools. Regras: metricas so das que bloqueiam/roteiam via agent_event, lifecycle ACTIVE->OBSERVE_ONLY->DEPRECATED->REMOVED por ablacao controlada, NUNCA auto-expiry por silencio, seguranca/autoridade/integridade nunca OBSERVE_ONLY. Serial por padrao: sem locks/integrador central/coordenacao worker-worker; readSet/writeSet/effectSet declarativos reconciliados contra bundle real (extensao C-008); paralelismo NAO construido F1 (gatilho: fila medida; exige 3 sets disjuntos; set indeterminavel => serial); orquestracao waterfall-by-handoff tipado, sem engine de workflow. Gate na Work Unit/composicao, nunca microcommit; EMENDA C-014 matriz de commit (writeSet!=vazio => exatamente 1; efeito sem write => receipt; no-op => baseTreeSha==resultTreeSha; falha => bundle descartado); HAR-1 delimitada a ActorRuns de construcao. Aceite amarrado ao release: digest de verificacao inclui changeId+contractRevisionDigest+snapshot de politica+matriz completa; executionContextDigest composicional por referencia (identidade C-002 + pins do manifest; exclui a si mesmo e digests de fechamento C-014 — aciclico); componente mudou => STALE => checkpoint+revalidacao. NAO construir com gatilhos: Mission engine, Milestone, fleet, validador universal/multi-provedor, IA de frescura, RAG de standards, warehouse de regras, auto-expiry, auto-aprovacao de contrato, marketplace, readiness score. Residuais de calibracao: tamanho WU x orcamento, caps, fix-ratio, cadencia de standards, tabela final de sinais. Convergido: pesquisa interna (5 varreduras MNFS/Mitra/Factory 07-2026/Conexus) + deep research externa 12/08 (SpecFirst, SWE-RPG, SpecPath, TDFlow, FixedBench, c-CRAB, CodeRabbit, Wink, Cursor, Amp; P6 confirma forte, P1-P5/P7 refinam, nada refutado) + Codex xhigh 7 rodadas (7,6->8,1->8,3->8,3->8,4->8,2->8,6). | [23](23-modelo-engenharia.md) |
