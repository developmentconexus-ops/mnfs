{
  "name": "conexus-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "test": "npm run test:repository",
    "test:repository": "node --test tests/repository/*.test.mjs",
    "repository:check": "node scripts/check-repository-hygiene.mjs && node scripts/check-doc-index.mjs && node scripts/check-current-state.mjs && node scripts/check-architecture-verification.mjs && node scripts/check-qualification-provenance.mjs",
    "wire:lint": "npx --yes @redocly/cli@2.47.0 lint contracts/api/product/openapi.yaml --extends=recommended",
    "wire:bundle": "npx --yes @redocly/cli@2.47.0 bundle contracts/api/product/openapi.yaml --output=/tmp/conexus-product-openapi.bundle.yaml",
    "wire:schema": "npx --yes ajv-cli@5.0.0 compile --spec=draft2020 -s contracts/api/project-operation.schema.json",
    "wire:verify": "npm run wire:lint && npm run wire:bundle && npm run wire:schema",
    "verify": "npm run repository:check && npm run test:repository && npm run wire:verify"
  }
}
