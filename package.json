{
  "name": "conexus-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "test": "npm run test:repository",
    "test:repository": "node --test tests/repository/*.test.mjs",
    "repository:check": "node scripts/check-repository-hygiene.mjs && node scripts/check-doc-index.mjs && node scripts/check-current-state.mjs && node scripts/check-qualification-provenance.mjs",
    "verify": "npm run repository:check && npm run test:repository"
  }
}
